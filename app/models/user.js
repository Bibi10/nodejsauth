var mysql      = require('mysql');
var MyAppModel  = require('mysql-model'); 
var connection = mysql.createConnection({
   host     : 'XXX.XXX.XX.XX',
   user     : 'root',
   password : 'M1r0o!',
   database : 'transcode'
 }); 

var users = 
 module.exports = connection;
